# Flappy Minimal

Минимальный Flappy Bird-like пример без ассетов.

Управление:

- `Space`, `ArrowUp`, `W`
- click / tap

Запуск: откройте `examples/flappy-minimal/index.html` через local server, например `npm run develop`.
