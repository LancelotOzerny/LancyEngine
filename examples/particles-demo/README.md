# Particles Demo

Рабочий пример `ParticleSystemComponent`.

- click/tap: explosion burst
- Space: pause/resume smoke emitter
- moving trail emitter

Запуск: `npm run develop`, затем открыть `/examples/particles-demo/`.
